#include <stdio.h>

/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: pointer array contain the arguments
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
