#include "main.h"

/**
  * mul -  function that multiplies two integers
  *
  *@a: the first number
  *
  *@b: the second number
  * Return: the result of multiply a by b
  */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
